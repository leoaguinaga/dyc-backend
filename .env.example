# Servidor
PORT=3333

# Base de datos (PostgreSQL)
DATABASE_URL=postgresql://user:password@host:5432/dyc?sslmode=require

# Frontend (para CORS)
FRONTEND_URL=http://localhost:3000

# better-auth
BETTER_AUTH_SECRET=cambia-esto-por-un-secreto-largo-y-aleatorio
BETTER_AUTH_URL=http://localhost:3333/api

# Email (SMTP corporativo, para notificaciones)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="Díaz y Castillo <notificaciones@tudominio.com>"
